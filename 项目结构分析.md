# Stormpot 项目工程结构分析

> 本文档基于对 `d:\yanyun\stormpot` 仓库源码的逐文件分析整理而成，涵盖工程布局、每个目录与文件的职责，以及核心架构设计。

---

## 1. 项目概览

| 项目 | 内容 |
| --- | --- |
| 项目名称 | Stormpot |
| 定位 | Java 高性能对象池（Object Pool）库，用于回收复用创建成本高昂的对象 |
| 版本 | 4.3-SNAPSHOT |
| GAV 坐标 | `com.github.chrisvest:stormpot` |
| 语言/版本 | Java 21+ |
| 许可证 | Apache License 2.0 |
| 维护者 | Chris Vest（chrisvest） |
| 版本策略 | 简单版本号：`X.Y`，Y+ 向后兼容，X+ 不向后兼容 |
| 核心特性 | 三种运行模式（Threaded / Inline / Direct）、线程本地缓存（TLR）、无锁快路径、精确泄漏检测、JMX 管理、虚拟线程支持、运行时热切换 Allocator、并行分配/释放、度量集成 |

---

## 2. 顶层目录结构

```
stormpot/
├── .github/                  # GitHub Actions CI/CD 工作流
├── .mvn/                     # Maven Wrapper 配置
├── .woodpecker/              # Woodpecker CI 流水线
├── benchmarks/               # JMH 基准测试模块
├── docs/                     # AsciiDoc 格式的用户文档（生成站点用）
├── src/                      # 主源码与测试源码
├── api-changes.json          # Revapi API 兼容性检查配置
├── docupdate.sh              # 文档站点发布脚本
├── license-header.txt        # 许可证头模板（license-maven-plugin 使用）
├── modular-pom.xml           # 聚合父 POM（stormpot + benchmarks）
├── mvnw / mvnw.cmd           # Maven Wrapper 启动脚本（Unix / Windows）
├── pom.xml                   # 主模块构建配置
├── README.adoc               # 项目首页文档（AsciiDoc 格式）
├── CONTRIBUTING.md           # 贡献指南（TDD 规范）
└── SECURITY.md               # 安全策略（受支持版本与漏洞上报）
```

---

## 3. 构建与工程配置文件

### 3.1 `pom.xml` — 主模块构建配置

Stormpot 主库的 Maven 构建描述，主要配置：

- **编译**：`maven-compiler-plugin`，Java 21（`<release>21`），启用 `-Xlint:all` 全部告警；
- **代码规范**：`maven-checkstyle-plugin` 在 `validate` 阶段检查 `src/main/java` 与 `src/test/java`，规则文件为 `src/etc/checkstyle.xml`，违反即构建失败；
- **许可头校验**：`license-maven-plugin` 用 `license-header.txt` 校验所有 `*.java` 文件头；
- **测试**：`maven-surefire-plugin`（单元测试）+ `maven-failsafe-plugin`（`*IT` 集成测试）；
- **覆盖率**：JaCoCo 生成测试覆盖率报告（`target/site/jacoco`）；
- **文档**：`asciidoctor-maven-plugin` 把 `docs/` 下 AsciiDoc 渲染为 HTML（输出到 `target/docs`），支持 `asciidoctor-diagram` 图表；
- **Javadoc**：`maven-javadoc-plugin` 生成 API 文档 jar，并通过 `--snippet-path` 引用 `src/test/java/examples/` 中的代码片段（`{@snippet ...}` 宏）；
- **API 兼容性**：`revapi-maven-plugin` 依据 `api-changes.json` 检查 API 变更是否违反语义化版本；
- **发布**：`central-publishing-maven-plugin` 发布到 Maven Central；`release` profile（`-DperformRelease`）下启用 GPG 签名；
- **其他**：`maven-source-plugin` 打包源码 jar、`maven-site-plugin` 生成项目站点、`maven-enforcer-plugin` 强制 Maven ≥ 3.9.6。

### 3.2 `modular-pom.xml` — 聚合父 POM

`stormpot-modular` 聚合模块，包含两个子模块：`benchmarks/` 与根目录 `.`（主库）。用于一次性构建并发布主库 + 基准测试模块。

### 3.3 `mvnw` / `mvnw.cmd` 与 `.mvn/wrapper/`

Maven Wrapper：无需本机安装 Maven 即可用固定版本构建。`mvnw` 为 Unix shell 脚本，`mvnw.cmd` 为 Windows 批处理，`.mvn/wrapper/` 存放 wrapper 配置与 maven-wrapper 依赖。

### 3.4 `license-header.txt`

Apache 2.0 许可证头模板，`license-maven-plugin` 用它校验/补齐所有 Java 文件顶部的版权声明（版权归 Chris Vest）。

### 3.5 `api-changes.json`

Revapi（API 变更检查工具）的配置文件：

- `revapi.versions`：启用语义化版本检查（major=破坏性、minor=非破坏、patch=等价）；
- `revapi.filter`：排除 `stormpot.internal` 包（内部实现不受 API 兼容约束）；
- `revapi.differences`：忽略已知误报差异（如 `PoolBuilder` 接口新增方法的 false positive）。

### 3.6 `docupdate.sh`

站点发布脚本：执行 `mvn clean verify site` 构建站点 → 切换到 `gh-pages` 分支 → 拷贝 `target/site` 与 `target/docs` → 修复 JaCoCo 资源路径 → 提交推送。

### 3.7 其他根文件

| 文件 | 作用 |
| --- | --- |
| `README.adoc` | 项目首页：特性列表、安装方式、快速上手、三要素（Poolable / Allocator / Expiration）说明 |
| `CONTRIBUTING.md` | 贡献指南：严格 TDD 实践、100% 覆盖率目标、PR 流程要求 |
| `SECURITY.md` | 安全策略：4.x 与 3.1.x 受支持；漏洞通过 GitHub Security 上报 |
| `.gitignore` | Git 忽略规则（target 等构建产物） |

---

## 4. 主源码 `src/main/java`

### 4.1 `module-info.java` — Java 模块声明

JPMS 模块描述：`module stormpot`，`requires transitive java.management`（JMX 支持），对外导出 `stormpot` 公开包；`stormpot.internal` 仅向测试模块 `stormpot.test` 开放（`exports ... to stormpot.test`）。

### 4.2 公开 API 包 `stormpot/`（13 个文件）

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `Pool.java` | `interface Pool<T> extends PoolTap<T>` | 池的顶层抽象：线程安全、可动态调整容量（`setTargetSize`）、可关闭（`shutdown`）。提供三个静态工厂：`from/fromThreaded`（后台线程分配）、`fromInline`（内联分配）、`of`（直接预分配模式） |
| `PoolTap.java` | `interface PoolTap<T>` | 从池中获取对象的入口抽象：`claim(Timeout)`（带超时）、`tryClaim()`（立即返回）、`claimInterruptibly` 等；Pool 继承它，但 Tap 本身不保证线程安全 |
| `PoolBuilder.java` | `sealed interface PoolBuilder<T> permits PoolBuilderImpl` | 池构建器：`size`、`expiration`、`backgroundExpirationEnabled`、`leakDetectionEnabled`、`optimizeForMemory`、`threadFactory`、`allocationConcurrency`、`metricsRecorder` 等配置项，`build()` 产出 `Pool`；支持 `clone()` 复制配置 |
| `Allocator.java` | `interface Allocator<T>` | 对象工厂接口：`allocate(Slot)` 创建对象、`deallocate(Poolable)` 销毁对象；还提供 `allocateAsync`/`deallocateAsync` 异步版本（返回 `CompletionStage`）。要求线程安全、中断安全 |
| `Reallocator.java` | `interface Reallocator<T> extends Allocator<T>` | 可复用对象的分配器：`reallocate(Slot, Poolable)` 尝试就地复用旧对象，减少老年代垃圾堆积 |
| `Poolable.java` | `interface Poolable` | 池化对象必须实现的接口：`release()` 时回调 `Slot.release(Poolable)` |
| `BasePoolable.java` | `class BasePoolable implements Poolable` | `Poolable` 的基础实现，持有 `protected final Slot slot`，子类只需构造时传槽位 |
| `Pooled.java` | `class Pooled<T> extends BasePoolable implements AutoCloseable` | 通用池化包装：同时实现 `Poolable` 与 `AutoCloseable`，可 try-with-resources 使用；`getObject()` 获取被池化的对象 |
| `Slot.java` | `interface Slot` | 池中位置的抽象：`release(Poolable)` 归还对象、`expire(Poolable)` 主动过期对象 |
| `SlotInfo.java` | `interface SlotInfo<T>` | 供 `Expiration` 查询的槽位信息：`getAgeMillis()`、`getCreatedNanoTime()`、`getStamp()`/`setStamp()`（存储自定义时间戳） |
| `Expiration.java` | `interface Expiration<T>` | 过期判定：`hasExpired(SlotInfo)`。提供 `after(t, u)`、`after(from, to, u)`（散布过期）、`every(...)`（周期检查）、`or(...)`（组合）、`never()` 等静态工厂 |
| `Timeout.java` | `final class Timeout` | 阻塞操作的最大等待时长：与单位无关（等价时长相等）、基于绝对时间；提供 `of(Duration)`、`of(ms, unit)` 工厂 |
| `ManagedPool.java` | `interface ManagedPool`（`@MXBean`） | JMX 管理接口：`getAllocationCount`、`getAllocationFailureCount`、`getObjectLeakCount`、`getTargetSize`、`setTargetSize` 等池管理操作与统计 |
| `MetricsRecorder.java` | `interface MetricsRecorder` | 操作度量上报接口：`recordAllocationLatency`、`recordAllocationFailureLatency`、`recordDeallocationLatency`、`recordReallocationLatency` 等；需线程安全、`record*` 方法不得抛异常。Stormpot 不提供默认实现（默认返回 `NaN`） |
| `Completion.java` | `interface Completion extends ManagedBlocker, Flow.Publisher<Void>` | 未来完成的抽象：类似无取消/结果的 Future；可阻塞等待（`await`/`block`）、可订阅（`Flow.Subscriber` 收到 `onComplete`）、支持 `OnAwait` 回调；用于池关闭与换分配器操作 |
| `LoggingAllocator.java` | `class LoggingAllocator<T> implements Allocator<T>` | 装饰器：包装任意 `Allocator`/`Reallocator`，将分配/释放耗时与结果记录到 `java.util.logging` |
| `PoolException.java` | `class PoolException extends RuntimeException` | 池运行时异常：分配失败（返回 null 或抛异常）、reallocate 失败、release 误用、Expiration 抛异常等场景抛出 |

### 4.3 内部实现包 `stormpot/internal/`（39 个文件）

该包为内部实现（`exports to stormpot.test`），不承诺 API 稳定。按逻辑分组如下：

#### ① 池核心实现（Pool Core）

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `BlazePool.java` | `final class implements Pool, ManagedPool` | 高性能池实现（心脏）：以 `LinkedTransferQueue<BSlot>` 作为 live 队列，配合线程本地 slot 缓存（TLR）加速反复 claim/release 的线程；分配/回收工作卸载给分配控制器；实现全部 claim 快/慢路径、关闭流程、目标容量调整 |
| `BlazePoolSingleThreadedTap.java` | `final class implements PoolTap` | 单线程 Tap：用实例字段（而非 ThreadLocal）持有 `BSlotCache`，无线程本地污染、更快；由 `getSingleThreadedTap()` 返回 |
| `BlazePoolThreadSafeTap.java` | `final class implements PoolTap` | 线程安全 Tap：无本地缓存，直接委托给池的 `claim`/`tryClaim`（内部仍走 TLR 快路径）；由 `getThreadSafeTap()` 返回 |
| `BlazePoolVirtualThreadSafeTap.java` | `final class implements PoolTap` | 虚拟线程 Tap：128 条 `BSlotCache` 条带按线程 id 哈希分配，避免数百万虚拟线程下 ThreadLocal 内存膨胀；懒加载单例；由 `getVirtualThreadSafeTap()` 返回 |

#### ② Slot 与缓存机制（Slot & Cache）

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `BSlot.java` | `sealed class implements Slot, SlotInfo, Task` | 池中对象的最小管理单元：四态状态机（DEAD→LIVING→CLAIMED/TLR_CLAIMED），用 VarHandle 无锁状态转换；持有 obj、poison（毒化异常）、leakCheck（泄漏检测引用）、allocator（记录归属分配器，支持热切换后正确释放） |
| `BSlotPadded.java` | `final class extends BSlot` | 填充版 BSlot：加 9 个 long 字段把每个槽隔离到独立 CPU 缓存行，避免伪共享（`optimizeForMemory=false` 时使用） |
| `BSlotCache.java` | `class` | 线程本地缓存载体：仅含一个 `BSlot slot` 字段 |
| `BSlotCachePadded.java` | `class extends BSlotCache` | 填充版缓存：14 个 long 填充让缓存指针独占缓存行 |
| `ThreadLocalBSlotCache.java` | `final class extends ThreadLocal<BSlotCache>` | 按 `optimizeForMemory` 决定提供普通或填充版缓存的 ThreadLocal |
| `RefillPile.java` | `final class extends AtomicReference<RefillSlot>` | 无等待（wait-free）槽栈：`push` 用 `getAndSet`，`pop` 用 CAS，`refill()` 一次性把整栈倾倒进 live 队列，批量降低队列争用 |
| `RefillSlot.java` | `final class` | RefillPile 栈的链表节点：持有一个 BSlot 与 next 引用 |

#### ③ 分配控制（Allocation Control）——三种运行模式的实现

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `AllocationController.java` | `abstract class` | 分配控制抽象基类：定义 offerDeadSlot、shutdown、switchAllocator、setTargetSize 及各计数器等抽象操作 |
| `AllocationProcess.java` | `abstract class` | 分配过程工厂：`threaded()` / `inline()` / `direct()` 三个静态方法分别产出三种模式的控制器 |
| `AllocationProcessMode.java` | `enum` | 三种运行模式枚举：`THREADED`、`INLINE`、`DIRECT` |
| `DirectAllocationController.java` | `final class extends AllocationController` | DIRECT 模式：构造时一次性预分配 size 个对象全部投入 live 队列，无后台线程、不可调容量；用于 `Pool.of(...)` |
| `InlineAllocationController.java` | `final class extends AllocationController` | INLINE 模式：分配/释放/过期检查都由 claim 线程在 claim 调用中同步内联完成，无后台线程；通过任务队列支持换分配器 |
| `ThreadedAllocationController.java` | `final class extends AllocationController` | THREADED 模式：薄壳，把全部操作委托给内部持有的 `BAllocThread` 并用 ThreadFactory 启动 |
| `BAllocThread.java` | `final class implements Runnable` | 专用后台分配线程（THREADED 模式核心）：事件循环持续补给池、处理死亡槽、执行过期检查、处理任务队列（Task 密封接口的四类元素）、处理换分配器请求、支持并行分配（allocationConcurrency）、执行关闭流程 |
| `AllocatorSwitch.java` | `record` | 换分配器请求：打包 `StackCompletion` 与新的 `Reallocator`，投递到控制器请求队列 |
| `AllocatorSwitchRequestPending.java` | `final class implements Task` | 单例哨兵任务：插入任务队列表示"有换分配器请求等待处理" |

#### ④ 构建器（Builder）

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `PoolBuilderImpl.java` | `final class implements PoolBuilder` | `PoolBuilder` 的唯一实现：持有全部配置项，用 `DEFAULTS`/`PERMISSIONS` 两张静态映射按模式初始化/校验；`build()` 创建 `BlazePool`；`getAdaptedReallocator()` 通过 `ReallocatingAdaptor.adapt` 把 Allocator 适配成 Reallocator；定义默认线程工厂（默认虚拟线程，`stormpot.usePlatformThreads` 系统属性可切换为平台线程） |
| `PoolBuilderDefaults.java` | `final class` | 各模式默认配置容器。注意：THREADED/INLINE 默认过期策略 `after(8, 10, MINUTES)`，DIRECT 默认 `never()` |
| `PoolBuilderPermissions.java` | `record` | 各模式允许修改的配置项权限：THREADED 全权限；INLINE 不支持线程工厂/后台过期/并发分配；DIRECT 只允许设 size 与泄漏检测 |

#### ⑤ 过期策略（Expiration）

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `TimeExpiration.java` | `final class implements Expiration` | 固定时长过期（`Expiration.after(t, u)` 的实现） |
| `TimeSpreadExpiration.java` | `final class implements Expiration` | 散布式过期（`Expiration.after(from, to, u)` 的实现）：为每个对象在区间内随机挑选过期时刻存入 `SlotInfo.setStamp()`，避免"惊群"式集体过期 |
| `EveryExpiration.java` | `final class implements Expiration` | 限频组合器（`Expiration.every(...)` 的实现）：时间窗内不重复检查内部 Expiration，查完推远截止时间 |
| `OrExpiration.java` | `final class implements Expiration` | 组合器（`Expiration.or(...)` 的实现）：任一子策略过期即过期 |

#### ⑥ 任务与完成机制（Task & Completion）

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `Task.java` | `sealed interface` | 密封任务接口，限定 BAllocThread 任务队列中允许出现的四类元素：`BSlot`、`AsyncAllocationCompletion`、`AsyncDeallocationCompletion`、`AllocatorSwitchRequestPending` |
| `AsyncAllocationCompletion.java` | `final class implements Task` | 异步分配完成信号：并行分配线程完成后把含新对象的槽交回主分配线程 |
| `AsyncDeallocationCompletion.java` | `final class implements Task` | 异步释放完成信号：把释放完的槽交回主线程，处理换代替换与后续回调 |
| `StackCompletion.java` | `final class implements Completion` | 基于无等待栈的 `Completion` 实现：支持 await/block 阻塞、Flow.Subscriber 订阅回调、OnAwait 回调；是池关闭、换分配器的完成载体 |
| `BaseSubscriber.java` | `class implements Flow.Subscriber<Void>` | `Flow.Subscriber` 空实现基类，省去订阅样板代码 |

#### ⑦ Reallocator 适配器（Adapter）

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `ReallocatingAdaptor.java` | `class implements Reallocator` | 把 `Allocator` 适配成 `Reallocator`：`reallocate` 先 deallocate（吞异常）再 allocate，并透传异步方法；静态 `adapt()` 按"是否已有 Reallocator、是否有 MetricsRecorder"选择包装层数 |
| `TimingReallocatingAdaptor.java` | `class extends ReallocatingAdaptor` | 计时版适配器：为分配/释放/异步操作测量耗时并上报 `MetricsRecorder` |
| `TimingReallocatorAdaptor.java` | `final class extends TimingReallocatingAdaptor` | 对已是 `Reallocator` 的对象做计时包装，额外测量 reallocate 的耗时与失败耗时 |

#### ⑧ 工具类（Utilities）

| 文件 | 类型 | 作用 |
| --- | --- | --- |
| `NanoClock.java` | `final class` | `System.nanoTime()` 封装与相对纳秒时长计算（elapsed、timeoutLeft） |
| `IdentityHashSet.java` | `final class implements Iterable<Object>` | 高性能身份去重集合：开放寻址 + 2-choice 哈希 + 8 元素桶 + stash + 树化降级 |
| `PreciseLeakDetector.java` | `final class` | 精确泄漏检测：为每个交给用户的 poolable 注册 `PhantomReference`，对象被 GC 而未先归还池时计数（`getObjectLeakCount`） |
| `PoisonException.java` | `final class extends Exception` | 特殊"毒化"异常：无堆栈、无抑制异常，用作常量（`SHUTDOWN_POISON`、`EXPLICIT_EXPIRE_POISON`）标记槽位/池的异常状态 |

### 4.4 资源与配置文件

| 路径 | 作用 |
| --- | --- |
| `src/main/resources/META-INF/native-image/com.github.chrisvest/stormpot/native-image.properties` | GraalVM Native Image 配置：`--initialize-at-build-time=stormpot.internal.BSlot,stormpot.internal.BlazePool`，解决 Graal 构建期初始化问题（临时方案，见 oracle/graal#3028） |
| `src/etc/checkstyle.xml` | Checkstyle 规则集：120 列行宽限制、禁止 tab、模块声明排除、`TreeWalker` 系列检查（命名、空白、Javadoc、final 等），用于主代码与测试代码的强制规范 |

---

## 5. 测试源码 `src/test/java`

### 5.1 `module-info.java` — 测试模块声明

`open module stormpot.test`：`requires stormpot` + JUnit 5（jupiter api/params/engine）、AssertJ、Dropwizard Metrics、HdrHistogram、Micrometer、java.sql（示例代码使用）。

### 5.2 `stormpot/tests/` — 白盒单元测试（直接测试 internal 包）

| 文件 | 测试对象 |
| --- | --- |
| `BSlotTest.java` | `BSlot` 状态机与释放协议 |
| `IdentityHashSetTest.java` | `IdentityHashSet` 工具类 |
| `LoggingAllocatorTest.java` | `LoggingAllocator` 装饰器 |
| `PoolBuilderReallocatingAdaptorTest.java` | 构建器与 Reallocator 适配链的协作 |
| `PreciseLeakDetectorTest.java` / `PreciseLeakDetectorIT.java` | 泄漏检测（单元 + 集成） |
| `ReallocatingAdaptorTest.java` | `ReallocatingAdaptor` 适配行为 |
| `RefillPileTest.java` | `RefillPile` 无等待栈 |
| `StackCompletionTest.java` 及 `StackCompletionWithCallbackTest`、`StackCompletionWithCompletionStageTest`、`StackCompletionWithOnAwaitTest` | `StackCompletion` 的 await/回调/CompletionStage/OnAwait 各用法 |
| `WhiteboxPoolBuilderTest.java` | `PoolBuilderImpl` 配置与校验 |
| `WhiteboxPoolTest.java` | `BlazePool` 内部机制（状态、计数、关闭等） |
| `MySubscriber.java` | 测试辅助：Flow.Subscriber 实现 |

### 5.3 `stormpot/tests/blackbox/` — 黑盒行为测试（经公开 API 验证）

| 文件 | 作用 |
| --- | --- |
| `AbstractPoolTest.java` | 抽象基类：参数化运行全部 Pool 行为测试（`@EnumSource(AllocationProcessMode)`），覆盖三种模式 |
| `AllocatorBasedPoolTest.java` | 基于 Allocator 的池行为 |
| `DefaultPoolTest.java` | 默认配置池的行为 |
| `DirectPoolTest.java` | DIRECT 模式（`Pool.of`）专测 |
| `ExpirationTest.java` | 过期策略行为 |
| `InlinePoolTest.java` | INLINE 模式专测 |
| `PoolBuilderTest.java` | 构建器公开行为 |
| `PooledTest.java` | `Pooled` 包装类行为 |
| `ThreadBasedPoolTest.java` / `ThreadBasedWithConcurrentAllocationPoolTest.java` | 多线程场景 + 并发分配场景 |
| `ThreadedPoolTest.java` | THREADED 模式专测 |
| `TimeoutTest.java` | `Timeout` 语义（等价时长、边界） |
| `Taps.java` | PoolTap 族（单线程/线程安全/虚拟线程）行为 |

### 5.4 `blackbox/simulations/` — 仿真测试

| 文件 | 作用 |
| --- | --- |
| `Sim.java` | 仿真抽象基类 |
| `ConstantTrafficBackgroundExpirationSim.java` | 恒定流量 + 后台过期仿真 |
| `VariableTrafficSim.java` | 可变流量仿真 |
| `TimeSpreadExpirationSim.java` | 散布过期策略的仿真验证 |
| `README.adoc` | 仿真测试说明文档 |

### 5.5 `blackbox/slow/` — 慢速集成测试（Failsafe，`*IT`）

| 文件 | 作用 |
| --- | --- |
| `PoolIT.java` | 通用池集成测试（容量调整、耗尽、关闭等全流程） |
| `DefaultPoolIT.java` / `ThreadedPoolIT.java` / `InlinePoolIT.java` / `ThreadBasedPoolIT.java` | 各模式/线程场景的长时间集成测试 |

### 5.6 `stormpot/tests/extensions/` — JUnit 5 扩展

| 文件 | 作用 |
| --- | --- |
| `ExecutorExtension.java` | 提供测试用 ExecutorService 的扩展 |
| `FailurePrinterExtension.java` | 测试失败时打印诊断信息 |
| `LongTimeout.java` | 慢速 CI 环境下的长超时注解/常量 |

### 5.7 `testkits/` — 测试工具包（跨测试类复用）

| 文件 | 作用 |
| --- | --- |
| `AlloKit.java` | 可编程 Allocator/Reallocator 工具：计数分配、可控延迟、失败注入、并发分配统计 |
| `DelegateAllocator.java` | 委托型 Allocator（便于替换行为） |
| `DelegateReallocator.java` | 委托型 Reallocator |
| `ExpireKit.java` | 可编程 Expiration 工具 |
| `FixedMeanMetricsRecorder.java` | 返回固定均值的 MetricsRecorder 桩 |
| `LastSampleMetricsRecorder.java` | 记录最后一次采样的 MetricsRecorder 桩 |
| `GarbageCreator.java` | 制造垃圾对象的工具（泄漏测试用） |
| `GenericPoolable.java` | 通用测试池化对象 |
| `NullSlot.java` | 空 Slot 桩 |
| `SlotInfoStub.java` | SlotInfo 桩 |
| `SomeRandomException.java` / `SomeRandomThrowable.java` | 随机异常/可抛错误，用于失败注入 |
| `ExpectedException.java` | 断言期望异常的工具 |
| `UnitKit.java` | 单元换算工具（毫秒/纳秒等） |

### 5.8 `examples/` — 示例代码（Javadoc 片段与集成示例）

| 文件 | 作用 |
| --- | --- |
| `Examples.java` | 主示例：被公开 API 的 Javadoc `{@snippet class=Examples region=...}` 宏引用（如 poolClaimExample、managedPoolExample），同时作为 JUnit 测试验证示例正确性 |
| `DaoPoolExample.java` | DAO 场景对象池示例 |
| `DropwizardMetricsRecorder.java` | Dropwizard Metrics 集成示例（`MetricsRecorder` 实现，被 Javadoc 引用） |
| `MicrometerBinderExample.java` | Micrometer 指标绑定示例 |
| `ThreadLocalWeakRefTest.java` | ThreadLocal 弱引用内存问题的演示/回归测试 |

### 5.9 `docs/`（测试目录）— 文档配套代码

| 文件 | 作用 |
| --- | --- |
| `MyAllocator.java` / `MyPoolable.java` / `MyOtherPoolable.java` | 教程文档中的示例类 |
| `MyApp.java` | 教程中的示例应用主类 |
| `package-info.java` | 该测试包说明 |

---

## 6. 基准测试 `benchmarks/`

JMH（Java Microbenchmark Harness）基准模块，`stormpot-benchmarks` 依赖主库与 `jmh-core`，经 `maven-shade-plugin` 打包为可执行 fat jar（主类 `org.openjdk.jmh.Main`），用 `java -jar benchmarks/target/benchmarks.jar` 运行。

| 文件 | 作用 |
| --- | --- |
| `ClaimRelease.java` | 核心基准：不同线程数/池大小下 claim-release 循环的吞吐与延迟（`@Threads(12)`、`-XX:+DebugNonSafepoints`） |
| `ThreadTransfer.java` | 跨线程传递基准：对象在线程间转移的争用成本（对比 JCTools 队列） |
| `GenericAllocator.java` | 通用测试 Allocator 基准实现 |
| `GenericPoolable.java` | 通用测试池化对象基准实现 |

---

## 7. 用户文档 `docs/`（AsciiDoc）

| 文件 | 内容 |
| --- | --- |
| `index.adoc` | 站点首页：内联 README，链接全部文档 |
| `usage.adoc` | 最简使用指南（快速上手） |
| `tutorial.adoc` | 从零开始的完整教程（概念 + 最佳实践） |
| `config.adoc` | 全部配置项参考（size、expiration、线程、泄漏检测等） |
| `memory.adoc` | API 的内存与并发语义参考（happens-before 关系） |
| `performance.adoc` | 性能调优指南（TLR、padding、批量分配等） |
| `trouble-shooting.adoc` | 故障模式与误用排查指南 |
| `jmx.adoc` | JMX 暴露与管理配置指南 |
| `metrics.adoc` | 指标与遥测库（Dropwizard/Micrometer）集成指南 |
| `releasing.adoc` | 版本发布流程说明 |

---

## 8. CI/CD 配置

### 8.1 `.github/workflows/` — GitHub Actions

| 文件 | 作用 |
| --- | --- |
| `maven.yml` | 主 CI：5 个 Java 版本（21–25）× 3 种发行版（temurin/semeru/graalvm）矩阵构建 `./mvnw verify`；另有 site 任务发布文档站点 |
| `codeql.yml` / `codeql-legacy.yml` | CodeQL 安全代码扫描 |
| `release.yml` | 正式版本发布工作流 |
| `snapshot-release.yml` | SNAPSHOT 版本发布工作流 |
| `dependabot.yml` | Dependabot 依赖更新配置 |

### 8.2 `.woodpecker/` — Woodpecker CI（镜像仓库用）

| 文件 | 作用 |
| --- | --- |
| `ci.yml` | Java 21/25 矩阵构建验证 |
| `benchmark.yml` | 基准测试流水线 |
| `site.yml` | 站点生成流水线 |

---

## 9. 核心架构设计

### 9.1 整体构建链路

```
用户代码
  │ Pool.fromThreaded(allocator) / fromInline / of
  ▼
PoolBuilderImpl ──(PoolBuilderDefaults / PoolBuilderPermissions 校验)──► AllocationProcess
  │ build()                                                              │ threaded()/inline()/direct()
  ▼                                                                       ▼
BlazePool ◄────────── AllocationController（三种实现之一）
  ├── live: LinkedTransferQueue<BSlot>           （就绪槽队列）
  ├── newAllocations / disregardPile: RefillPile （无等待槽栈，共享 live 队列）
  ├── tlr: ThreadLocalBSlotCache                 （线程本地缓存）
  └── BAllocThread（仅 THREADED 模式）            （后台分配/回收事件循环）
```

### 9.2 三种运行模式

| 模式 | 工厂方法 | 控制器 | 特点 |
| --- | --- | --- | --- |
| THREADED | `Pool.fromThreaded` / `Pool.from` | `ThreadedAllocationController` + `BAllocThread` | 后台线程负责分配/释放/过期检查；claim 超时永远被遵守；可并行分配、可后台过期、可换分配器、可动态调容量 |
| INLINE | `Pool.fromInline` | `InlineAllocationController` | 无后台线程，claim 线程内联完成分配/释放/过期检查；省线程但慢分配会阻塞 claim |
| DIRECT | `Pool.of` | `DirectAllocationController` | 构造时一次性预分配全部对象；无线程、不可调容量、默认不过期；最快 |

### 9.3 BSlot 状态机

```
         allocator.allocate()           claim 成功
DEAD ───────────────────────► LIVING ───────────────► CLAIMED / TLR_CLAIMED
  ▲                              │                        │
  │      deallocate()            │       release()        │
  └──────────────────────────────┴────────────────────────┘
        （经 offerDeadSlot 交控制器）        （release 后回 live 队列）
```

状态转换全部通过 `VarHandle` 无锁 CAS 完成；重复 release 抛 `PoolException`；过期/毒化槽由控制器执行 deallocate。

### 9.4 claim 的快路径与慢路径

- **快路径**：先尝试线程本地缓存槽的 `live2claimTlr()` CAS，成功后做有效性检查（毒化/过期），命中即返回——无锁、无队列争用；
- **慢路径**：依次从 `newAllocations.pop()` → `live.poll()` 取槽，取不到则 `disregardPile.refill()` 批量倾倒死槽回队列；无效槽 push 回 `disregardPile`；仍无则触发分配并等待（带超时）。

### 9.5 关键机制

- **热切换分配器**：`Pool.switchAllocator(reallocator)` 投递 `AllocatorSwitch` 请求（含 `StackCompletion` 完成信号），`BAllocThread` 处理后逐代替换对象，`BSlot.allocator` 保证旧对象由原分配器释放；
- **泄漏检测**：`PreciseLeakDetector` 用 `PhantomReference` + `IdentityHashSet` 在近乎零开销下精确统计泄漏对象数；
- **虚拟线程适配**：`BlazePoolVirtualThreadSafeTap` 以 128 条缓存条带替代 ThreadLocal，规避海量虚拟线程的内存膨胀；
- **性能优化**：`BSlotPadded`/`BSlotCachePadded` 缓存行填充防伪共享；`RefillPile` 批量入队降争用；`TimeSpreadExpiration` 随机散布过期时间防惊群。

---

## 10. 构建与运行

```bash
# 构建主库并运行全部测试（单元 + 集成）
./mvnw verify

# 跳过 Javadoc 快速验证
./mvnw -Dmaven.javadoc.skip=true verify

# 生成站点与文档（target/site、target/docs）
./mvnw site

# 运行 JMH 基准测试（需先打包）
cd benchmarks
../mvnw package
java -jar target/benchmarks.jar
```

---

*本文档由对源码的逐文件分析自动生成，生成日期：2026-08-11。*
